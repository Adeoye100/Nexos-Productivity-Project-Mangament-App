# Nexos Dashboard - Frontend Environment Variables

# The port the dev server should run on
PORT=5173

# The base path for the application (usually /)
BASE_PATH=/

# Backend API target for proxying (dev only)
# VITE_API_URL=http://localhost:4000
